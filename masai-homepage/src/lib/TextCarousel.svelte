<script>
    import { onMount, onDestroy } from "svelte";
    let currentIndex = 0;
    let interval;

    // function nextSlide() {
    //     currentIndex = (currentIndex + 1) % images.length;
    // }

    // onMount(() => {
    //     interval = setInterval(nextSlide, 3000);
    // });

    // onDestroy(() => {
    //     clearInterval(interval);
    // });
</script>

<div
    class="m-auto top-0 left-0  h-[200px] flex justify-center items-center"
>

</div>
